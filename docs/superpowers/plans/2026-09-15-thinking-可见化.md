# thinking 实时可见化 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** thinking 模型的推理过程在 Web UI 与 TUI 上实时可见，同时消除首 token 前与工具轮之间的静默空窗。

**Architecture:** 协议层新增 `Event::Reasoning`（内联事件，走 `RunSink::Conn`，不落库），Web UI 用一个不进 `messageMap` 的单例活动卡渲染，TUI 在状态栏滚动显示。前端状态机抽成零依赖纯 JS 模块，用 Node 内置 `node --test` 测，不引入 vitest。

**Tech Stack:** Rust（tokio/axum/ratatui/crossterm）、Vue 3（Vite）、Node 24 内置 test runner、`unicode-width`（新增为 oc-tui 直接依赖）。

## Global Constraints

- Rust 1.90（workspace 与 CI 均锁此版本）；`cargo clippy --workspace --all-targets -- -D warnings` 必须通过（CI 会跑）。
- **本仓不 rustfmt**：CI 注释明确「暂不加 cargo fmt --check，从未整体 rustfmt 过」。改动的 Rust 文件保持现有缩进风格，不要顺手重排。
- **thinking 不落库**：不加迁移、不加 `entry` 列、不改 history 接口、不改 `session.rs:642` 的 `reasoning: None`（那行及注释保持原样）。
- **不改 OpenAI 兼容层 `/v1/responses` 的代码**：它已有 `Some(_) => {}` 兜底，`Reasoning` 自动静默。只加测试锁契约。
- **不加 client 能力协商**：项目未上生产，所有 client 在本仓库内一起改。
- 前端测试命令统一为 `node --test`，**不引入 vitest / 任何新 npm 依赖**。
- 提交信息用中文 conventional commits（feat/fix/docs/test），与现有 git log 一致。
- 效果图 `docs/superpowers/specs/2026-09-15-thinking-ui-mockup.html` 已存在，与本计划无关，不要动它。

---

## File Structure

| 文件 | 职责 | 本计划动作 |
|---|---|---|
| `crates/oc-proto/src/event.rs` | 协议事件枚举 | 加 `Reasoning` 变体 |
| `crates/oc-server/src/run.rs` | run 主循环 | `Delta::Reasoning` 分支加 `emit_inline` |
| `crates/oc-http/src/native/chat.rs` | native SSE 透传 | `belongs()` / `event_name()` 加 reasoning |
| `crates/oc-tui/src/app.rs` | TUI 应用 | 状态栏滚动 + 键位 + 加 `Reasoning` 分支 |
| `crates/oc-tui/src/client.rs` | NDJSON 解码 | 坏帧 skip 而非断连 |
| `crates/oc-tui/Cargo.toml` | TUI 依赖 | 加 `unicode-width` |
| `crates/oc-http/ui/src/lib/api.js` | API 客户端 | `sendChat` 加 `onReasoning` 回调 |
| `crates/oc-http/ui/src/lib/activity.js` | **新**：活动卡状态机（纯 JS） | 新建 |
| `crates/oc-http/ui/src/lib/activity.test.js` | **新**：状态机测试 | 新建 |
| `crates/oc-http/ui/src/lib/state.js` | 全局状态 | 加 `activities` / `activityFor` |
| `crates/oc-http/ui/src/components/LiveActivity.vue` | **新**：活动卡组件 | 新建 |
| `crates/oc-http/ui/src/components/ChatPane.vue` | 聊天面板 | 挂卡 + 定时器 + 回调接线 |
| `crates/oc-http/ui/src/components/StatusBar.vue` | 状态栏 | `active_run` 改派生 |
| `docs/reference/protocol.md` | 协议文档 | F 节：第三方接入 |

---

### Task 1: 协议新增 `Event::Reasoning` + 发出点

**Files:**
- Modify: `crates/oc-proto/src/event.rs`
- Modify: `crates/oc-server/src/run.rs:591-595`
- Modify: `crates/oc-http/src/native/chat.rs:130-154`（`belongs` + `event_name`）
- Modify: `crates/oc-tui/src/app.rs`（`handle_event` 与 `event_session` 加分支）
- Test: `crates/oc-server/tests/reasoning_events.rs`（新建）

**Interfaces:**
- Produces: `Event::Reasoning { session: SessionId, run_id: RunId, delta: String }` —— 后续 Task 3/5/8 消费。
- 注意：本 task 必须把 `Event` 的所有穷举 match 点补齐，否则 workspace 编译失败。已核实消费点仅两处：`oc-tui/src/app.rs`（`handle_event` 的 match + `event_session` 函数，均**无** `_` 兜底）；`oc-http/src/native/chat.rs` 的 `belongs()` 与 `event_name()`（`event_name` 有 `_ =>` 吗？无，穷举）。`oc-http/src/server.rs:181` 与 `native/events.rs` 有 `Some(_)`/`continue` 兜底，**无需改**。

- [ ] **Step 1: 加 `Reasoning` 变体**

在 `crates/oc-proto/src/event.rs` 的 `Event` enum 里，`Assistant` 变体之后插入：

```rust
    /// thinking 模型的推理增量。与 Assistant 分开：它不是可见回答，
    /// 只供客户端实时展示，不落库、不参与 prompt 重放。
    Reasoning {
        session: SessionId,
        run_id: RunId,
        delta: String,
    },
```

- [ ] **Step 2: 发出点**

`crates/oc-server/src/run.rs` 的 `Delta::Reasoning(r)` 分支（现为 `reasoning.push_str(&r);`）改为：

```rust
            Delta::Reasoning(r) => {
                reasoning.push_str(&r);
                // 同时推给客户端实时展示。与 Assistant 同走 inline 通道，
                // emit_inline 内部在连接断开时会 cancel，无需显式处理返回值。
                let _ = emit_inline(
                    ctx,
                    Event::Reasoning {
                        session: ctx.session_id.clone(),
                        run_id: ctx.run_id.clone(),
                        delta: r.clone(),
                    },
                )
                .await;
            }
```

- [ ] **Step 3: 补齐 oc-http 消费点**

`crates/oc-http/src/native/chat.rs`：

`belongs()` 里，把 `Event::Assistant { run_id: r, .. }` 那组扩展成：

```rust
        Event::Lifecycle { run_id: r, .. }
        | Event::Assistant { run_id: r, .. }
        | Event::Reasoning { run_id: r, .. }
        | Event::Tool { run_id: r, .. }
        | Event::Approval { run_id: r, .. }
        | Event::UserInput { run_id: r, .. } => r == run_id,
```

`event_name()` 里加：

```rust
        Event::Reasoning { .. } => "reasoning",
```

- [ ] **Step 4: 补齐 oc-tui 消费点**

`crates/oc-tui/src/app.rs`：

`event_session()` 函数加：

```rust
        | Event::Reasoning { session, .. }
```

`handle_event` 的 match 末尾（`Event::Usage` 分支之后）加一个临时空分支（Task 8 会替换成滚动逻辑）：

```rust
            Event::Reasoning { .. } => {}
```

- [ ] **Step 5: 写 server 测试**

新建 `crates/oc-server/tests/reasoning_events.rs`：

```rust
//! 协议回归：thinking 模型的 reasoning 增量要推成 `Event::Reasoning`，
//! 同时同轮回喂的 `Message.reasoning` 必须保留（DeepSeek 400 回归保护）。

use std::sync::Arc;
use std::time::Duration;

use oc_llm::mock::{ScriptStep, SequencedMock};
use oc_llm::types::ToolCallDelta;
use oc_llm::{Delta, FinishReason};
use oc_proto::{Event, LifecyclePhase};
use oc_server::session::{self, SessionConfig};
use oc_server::testing::{test_cfg, SessionConfigExt};
use oc_server::tools_bridge::ToolExecutor;
use oc_tools::exec::ExecTool;
use oc_tools::shell::Shell;
use oc_tools::ToolRegistry;
use tokio::sync::broadcast;

fn reasoning_step(r: &str) -> Vec<ScriptStep> {
    vec![
        ScriptStep { delay: Duration::ZERO, delta: Delta::Reasoning(r.into()) },
        ScriptStep { delay: Duration::ZERO, delta: Delta::Text("回复".into()) },
        ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::Stop) },
    ]
}

/// 带工具调用的轮：reasoning 必须绑在带 tool_calls 的 assistant 消息上回喂。
fn reasoning_tool_step(r: &str) -> Vec<ScriptStep> {
    vec![
        ScriptStep { delay: Duration::ZERO, delta: Delta::Reasoning(r.into()) },
        ScriptStep {
            delay: Duration::ZERO,
            delta: Delta::ToolCall(ToolCallDelta {
                call_id: "call-1".into(),
                name: Some("exec".into()),
                args_chunk: r#"{"command":"echo hi"}"#.into(),
            }),
        },
        ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::ToolUse) },
    ]
}

fn text_step(t: &str) -> Vec<ScriptStep> {
    vec![
        ScriptStep { delay: Duration::ZERO, delta: Delta::Text(t.into()) },
        ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::Stop) },
    ]
}

async fn collect_until_terminal(
    rx: &mut broadcast::Receiver<Event>,
    timeout: Duration,
) -> Vec<Event> {
    let mut out = Vec::new();
    let deadline = tokio::time::Instant::now() + timeout;
    while let Ok(Ok(ev)) = tokio::time::timeout_at(deadline, rx.recv()).await {
        let terminal = matches!(
            ev,
            Event::Lifecycle { phase: LifecyclePhase::End, .. }
                | Event::Lifecycle { phase: LifecyclePhase::Error { .. }, .. }
        );
        out.push(ev);
        if terminal {
            break;
        }
    }
    out
}

#[tokio::test]
async fn reasoning_delta_emits_reasoning_event() {
    let (tx, mut rx) = broadcast::channel(512);
    let provider = Arc::new(SequencedMock::new(vec![reasoning_step("我需要先确认路径")]));
    let sid = oc_proto::SessionId::main();
    let handle = session::spawn(
        sid.clone(),
        test_cfg(),
        provider,
        tx,
        oc_store::Store::open_memory().unwrap(),
        oc_server::diag::DiagRegistry::new().for_session(&sid),
    );
    handle.submit("写文件".into(), handle.broadcast_sink()).await.expect("run");
    let evs = collect_until_terminal(&mut rx, Duration::from_secs(5)).await;

    let reasoning: String = evs
        .iter()
        .filter_map(|e| match e {
            Event::Reasoning { delta, .. } => Some(delta.as_str()),
            _ => None,
        })
        .collect();
    assert_eq!(reasoning, "我需要先确认路径", "reasoning 应逐 delta 透出，事件流：{evs:?}");
}

#[tokio::test]
async fn reasoning_is_carried_back_on_tool_turn() {
    // 工具轮：reasoning 必须绑在带 tool_calls 的 assistant 消息上回喂，
    // 否则 DeepSeek 400。这条锁住 run.rs:264 那段的回归。
    let mut reg = ToolRegistry::new();
    reg.register(Arc::new(ExecTool::new(
        oc_core::tool::ApprovalMode::Allow,
        Duration::from_secs(10),
        Duration::from_secs(30),
        Shell::resolve().unwrap(),
    )));
    let tools = ToolExecutor::new(Arc::new(reg));

    let provider = Arc::new(SequencedMock::new(vec![
        reasoning_tool_step("我先跑个命令看看"),
        text_step("跑完了"),
    ]));
    let sid = oc_proto::SessionId::main();
    let store = oc_store::Store::open_memory().unwrap();
    let (tx, mut rx) = broadcast::channel(512);
    let handle = session::spawn(
        sid.clone(),
        test_cfg().with_tools(tools),
        provider.clone(),
        tx,
        store,
        oc_server::diag::DiagRegistry::new().for_session(&sid),
    );
    handle.submit("跑个命令".into(), handle.broadcast_sink()).await.expect("run");
    collect_until_terminal(&mut rx, Duration::from_secs(10)).await;

    let caps = provider.captures();
    let cap = caps.lock().unwrap();
    let first = cap.first().expect("至少一轮请求");
    let carried = first
        .messages
        .iter()
        .any(|m| m.role == oc_llm::MsgRole::Assistant
            && !m.tool_calls.is_empty()
            && m.reasoning.as_deref() == Some("我先跑个命令看看"));
    assert!(carried, "工具轮回喂必须带 reasoning，实际消息：{:?}", first.messages);
}
```

- [ ] **Step 6: 跑测试验证通过**

Run: `cargo test -p oc-server --test reasoning_events`
Expected: 2 passed。

- [ ] **Step 7: 全 workspace 编译 + 既有测试**

Run: `cargo test --workspace --locked`
Expected: 全绿（新增变体后所有 match 已补齐，无编译错误）。

- [ ] **Step 8: Commit**

```bash
git add crates/oc-proto/src/event.rs crates/oc-server/src/run.rs \
        crates/oc-http/src/native/chat.rs crates/oc-tui/src/app.rs \
        crates/oc-server/tests/reasoning_events.rs
git commit -m "feat(proto): 新增 Event::Reasoning，run 内 reasoning 实时透出"
```

---

### Task 2: TUI 解码层兜底（坏帧不断连）

**Files:**
- Modify: `crates/oc-tui/src/client.rs:104-118`
- Test: `crates/oc-tui/src/client.rs`（追加 `#[cfg(test)]` 模块）

**Interfaces:**
- Produces: `fn decode_frame(line: &str) -> Result<Option<Frame>>` —— 供 `recv()` 内部使用，Task 8 不依赖它，纯粹是健壮性。

- [ ] **Step 1: 抽出解码纯函数**

`crates/oc-tui/src/client.rs`，把 `recv()` 循环里的解码逻辑抽成模块内函数，并加宽容处理：

```rust
/// 解码一行 NDJSON。空行返回 `Ok(None)`（跳过）；坏帧 skip + warn 后也返回
/// `Ok(None)`，不让一条脏行杀掉整条连接（对齐 oc-http `conn_pool.rs` 的宽容策略）。
fn decode_frame(line: &str) -> Result<Option<Frame>> {
    let t = line.trim_end();
    if t.is_empty() {
        return Ok(None);
    }
    match serde_json::from_str::<Frame>(t) {
        Ok(f) => Ok(Some(f)),
        Err(e) => {
            eprintln!("oc-tui: 跳过无法解析的帧（{e}）");
            Ok(None)
        }
    }
}
```

`recv()` 循环改为：

```rust
    pub async fn recv(&mut self) -> Result<Option<Frame>> {
        loop {
            self.line.clear();
            let n = self.reader.read_line(&mut self.line).await?;
            if n == 0 {
                return Ok(None);
            }
            if let Some(frame) = decode_frame(&self.line)? {
                return Ok(Some(frame));
            }
            // decode_frame 返回 None：空行或坏帧，继续读下一行。
        }
    }
```

（原实现里 `read_line` 读空行后 `continue` 的逻辑也由 `decode_frame` 的 `Ok(None)` 承接，语义不变。）

- [ ] **Step 2: 写单测**

`crates/oc-tui/src/client.rs` 末尾追加：

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use oc_proto::{Event, LifecyclePhase, SessionId};

    #[test]
    fn decode_skips_unknown_frame_kind() {
        // 服务端将来新增的 kind 不该让旧 client 断连。
        let r = decode_frame(r#"{"kind":"future_kind","x":1}"#);
        assert!(r.unwrap().is_none(), "未知 kind 应被跳过");
    }

    #[test]
    fn decode_skips_empty_and_malformed_lines() {
        assert!(decode_frame("   ").unwrap().is_none(), "空行应跳过");
        assert!(decode_frame("{not json").unwrap().is_none(), "坏 JSON 应跳过");
    }

    #[test]
    fn decode_parses_valid_event() {
        let f = decode_frame(
            r#"{"kind":"event","event":{"event":"lifecycle","session":"main","run_id":"r1","phase":"end"}}"#,
        )
        .unwrap()
        .expect("合法帧应解析");
        match f {
            Frame::Event(Event::Lifecycle { phase: LifecyclePhase::End, .. }) => {}
            other => panic!("应解析成 lifecycle end，得到 {other:?}"),
        }
    }
}
```

- [ ] **Step 3: 跑测试**

Run: `cargo test -p oc-tui`
Expected: 3 passed（连同既有 `normalize_key` 测试）。

- [ ] **Step 4: Commit**

```bash
git add crates/oc-tui/src/client.rs
git commit -m "fix(tui): 坏帧 skip 而非断连，对齐 http 层的宽容策略"
```

---

### Task 3: native API 的 reasoning SSE 透出

**Files:**
- Modify: `crates/oc-http/tests/native.rs`（`ambient_stream_excludes_inline_run_events` 扩展）
- Test: `crates/oc-http/tests/native.rs`（新增两个测试）

**Interfaces:**
- Consumes: `Event::Reasoning`（Task 1）。`belongs()`/`event_name()` 已在 Task 1 改好，本 task 只加测试锁线上格式。

- [ ] **Step 1: 加 chat/send SSE 事件名断言**

`crates/oc-http/tests/native.rs` 末尾追加（复用文件里已有的 `spawn_gateway` / `read_sse` / `event_names` 与 `TestDaemon`）：

```rust
/// reasoning 应作为内联事件出现在 chat/send 的 SSE 流上，事件名是 `reasoning`。
#[tokio::test]
async fn chat_send_streams_reasoning_events() {
    use oc_llm::mock::{MockProvider, ScriptStep};
    let daemon = TestDaemon::start(
        "nat-reas",
        Arc::new(MockProvider::scripted(vec![
            ScriptStep { delay: Duration::from_millis(20), delta: Delta::Reasoning("先想想".into()) },
            ScriptStep { delay: Duration::from_millis(20), delta: Delta::Text("正文".into()) },
            ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::Stop) },
        ])),
    )
    .await;
    let base = spawn_gateway(daemon.transport(), 8).await;
    let client = reqwest::Client::new();

    let resp = client
        .post(format!("{base}/api/v1/chat/send"))
        .json(&serde_json::json!({ "session": "nat-reas-s", "text": "hi" }))
        .send()
        .await
        .expect("send 应有应答");
    let body = read_sse(resp, Duration::from_secs(20)).await;

    let names = event_names(&body);
    assert!(names.iter().any(|n| n == "reasoning"), "SSE 应含 reasoning 事件，实际：{names:?}");
    assert!(body.contains("先想想"), "reasoning 正文应透传，实际：{body}");
    assert!(body.contains(r#""event":"reasoning""#), "data 里应带 event 标签：{body}");
}
```

- [ ] **Step 2: ambient 流排除 reasoning 的断言**

扩展 `native.rs` 里已有的 `ambient_stream_excludes_inline_run_events` 测试：它当前断言 ambient 流不含 assistant delta。在 `post_body` 读取之后，加一行对 ambient 缓冲的断言。找到该测试里对 `ambient_buf` 的最终断言处，追加：

```rust
    assert!(
        !ambient_buf.contains("reasoning"),
        "reasoning 是内联事件，绝不该出现在 ambient 流：{ambient_buf}"
    );
```

（若该测试的模型脚本不含 reasoning，先把它换成同时产出 reasoning + text 的脚本，让断言有东西可证伪。修改 `TestDaemon::start` 的脚本为：

```rust
    ScriptStep { delay: Duration::from_millis(50), delta: Delta::Reasoning("内联思考".into()) },
    ScriptStep { delay: Duration::from_millis(50), delta: Delta::Text("内联文本".into()) },
    ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::Stop) },
```

并把对 `post_body.contains("内联文本")` 的断言保留。）

- [ ] **Step 3: 跑测试**

Run: `cargo test -p oc-http --test native`
Expected: 全绿（含新增 `chat_send_streams_reasoning_events` 与扩展后的 ambient 测试）。

- [ ] **Step 4: Commit**

```bash
git add crates/oc-http/tests/native.rs
git commit -m "test(http): reasoning 走 chat/send 内联流、不进 ambient 流"
```

---

### Task 4: `/v1/responses` 静默锁契约

**Files:**
- Test: `crates/oc-http/tests/web.rs`（追加一个测试）

**Interfaces:**
- Consumes: `Event::Reasoning`（Task 1）。代码零改动——`server.rs:181` 的阻塞路径有 `Some(_) => {}` 兜底，`sse.rs` 的 `event_to_sse` 也有 `_ => None` 兜底，reasoning 自动静默。本 task 纯加测试锁死这个契约。

- [ ] **Step 1: 写测试**

`crates/oc-http/tests/web.rs` 末尾追加。需要读非流式 Responses 的 body，验证其中没有 reasoning 文本。`MockProvider::echo_text` 只回显文本，不含 reasoning；这里需要 scripted provider 产出 reasoning + text，再断言 Responses 输出里只有 text。参考文件顶部的 `spawn_with`，但它写死了 `echo_text`，本测试需要另一个 spawn 变体：

```rust
use oc_llm::mock::ScriptStep;
use oc_llm::{Delta, FinishReason};

/// Responses 协议没有 thinking 事件：reasoning 必须被静默，只吐可见文本。
#[tokio::test]
async fn responses_endpoint_silently_drops_reasoning() {
    use oc_llm::mock::MockProvider;
    let daemon = TestDaemon::start(
        "web-reas",
        Arc::new(MockProvider::scripted(vec![
            ScriptStep { delay: Duration::from_millis(20), delta: Delta::Reasoning("隐藏的思考".into()) },
            ScriptStep { delay: Duration::from_millis(20), delta: Delta::Text("可见回答".into()) },
            ScriptStep { delay: Duration::ZERO, delta: Delta::Done(FinishReason::Stop) },
        ])),
    )
    .await;
    let pool = oc_http::conn_pool::ConnPool::new(daemon.transport(), 4, 4);
    let app = oc_http::create_app(pool, "mock".into(), oc_http::AppConfig { token: None, web_ui: false });
    let listener = tokio::net::TcpListener::bind("127.0.0.1:0").await.expect("绑端口");
    let addr = listener.local_addr().expect("取端口");
    tokio::spawn(async move { let _ = axum::serve(listener, app).await; });
    let base = format!("http://{addr}");

    let client = reqwest::Client::new();
    let body = client
        .post(format!("{base}/v1/responses"))
        .json(&serde_json::json!({ "input": "hi", "stream": false }))
        .send()
        .await
        .expect("应有应答")
        .text()
        .await
        .expect("读 body");
    assert!(body.contains("可见回答"), "Responses 应含可见文本：{body}");
    assert!(!body.contains("隐藏的思考"), "reasoning 不得泄漏进 Responses：{body}");
}
```

- [ ] **Step 2: 跑测试**

Run: `cargo test -p oc-http --test web`
Expected: 全绿。

- [ ] **Step 3: Commit**

```bash
git add crates/oc-http/tests/web.rs
git commit -m "test(http): /v1/responses 静默丢弃 reasoning"
```

---

### Task 5: 活动卡状态机（纯 JS）

**Files:**
- Create: `crates/oc-http/ui/src/lib/activity.js`
- Create: `crates/oc-http/ui/src/lib/activity.test.js`

**Interfaces:**
- Produces（Task 6 依赖，名字/字段必须一致）：
  - `export const WAIT_THRESHOLD_MS = 400`
  - `export function createActivity()` 返回 plain object：
    - 字段：`state`（`'idle' | 'armed' | 'waiting' | 'thinking' | 'hidden'`）、`emptySince`（epoch ms | null）、`waitingSince`（epoch ms | null）、`thinkingSince`（epoch ms | null）、`thinkingText`（string）
    - 方法：`arm(now)`、`showWaiting(now)`、`reasoning(delta, now)`、`visible()`、`toolEnd(now)`、`end()`
  - 状态机**同步纯函数、不持定时器**；延迟由调用方（ChatPane）用 `setTimeout(WAIT_THRESHOLD_MS)` 触发 `showWaiting`。

**语义说明**（实现与测试都要遵循）：
- `arm(now)`：一次空窗开始（提交后 / 工具结束后）。设 `emptySince = now`，`state = 'armed'`。
- `showWaiting(now)`：阈值到点由调用方调用。仅当 `state === 'armed'` 时生效 → `state = 'waiting'`、`waitingSince = now`。若期间已 `reasoning`/`visible`，则 no-op。
- `reasoning(delta, now)`：首个 reasoning delta 时 `state = 'thinking'`、`thinkingSince = now`；后续 delta 只追加 `thinkingText += delta`。
- `visible()`：本 step 首个可见产物（assistant delta 或 tool start）→ `state = 'hidden'`，清空 `thinkingText`、`thinkingSince = null`。
- `toolEnd(now)`：工具结束且 run 未终 → 回到空窗，等价 `arm(now)`。
- `end()`：`state = 'idle'`，全部字段归零。

- [ ] **Step 1: 写状态机**

`crates/oc-http/ui/src/lib/activity.js`：

```js
/**
 * 单例活动卡的状态机。零依赖、同步纯函数，不持任何定时器——
 * 延迟由调用方用 setTimeout 触发 showWaiting()，这样 node --test 可直接断言
 * 状态迁移，无需 fake timer。
 *
 * state: 'idle' | 'armed' | 'waiting' | 'thinking' | 'hidden'
 *   armed    空窗开始，未达阈值（不渲染）
 *   waiting  空窗超过阈值（渲染「等待模型」）
 *   thinking 收到 reasoning delta（渲染「思考中」）
 *   hidden   本 step 首个可见产物已到（工具/文本接管）
 */

export const WAIT_THRESHOLD_MS = 400;

export function createActivity() {
  return {
    state: 'idle',
    emptySince: null,     // 本次空窗起点
    waitingSince: null,   // waiting 显示起点
    thinkingSince: null,
    thinkingText: '',

    arm(now) {
      this.state = 'armed';
      this.emptySince = now;
      this.waitingSince = null;
      this.thinkingSince = null;
      this.thinkingText = '';
    },

    showWaiting(now) {
      if (this.state !== 'armed') return;   // 已被 reasoning/visible 打断
      this.state = 'waiting';
      this.waitingSince = now;
    },

    reasoning(delta, now) {
      const first = this.state !== 'thinking';
      this.state = 'thinking';
      if (first) this.thinkingSince = now;
      this.thinkingText += delta;
    },

    visible() {
      this.state = 'hidden';
      this.thinkingSince = null;
      this.thinkingText = '';
    },

    toolEnd(now) {
      this.arm(now);
    },

    end() {
      this.state = 'idle';
      this.emptySince = null;
      this.waitingSince = null;
      this.thinkingSince = null;
      this.thinkingText = '';
    },
  };
}
```

- [ ] **Step 2: 写测试**

`crates/oc-http/ui/src/lib/activity.test.js`：

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import { createActivity, WAIT_THRESHOLD_MS } from './activity.js';

test('提交后 arm，阈值前不 waiting', () => {
  const a = createActivity();
  a.arm(1000);
  assert.equal(a.state, 'armed');
  a.showWaiting(1000 + WAIT_THRESHOLD_MS - 1);
  assert.equal(a.state, 'waiting');
});

test('reasoning 直接转 thinking，跳过 waiting', () => {
  const a = createActivity();
  a.arm(1000);
  a.reasoning('先想想', 1000 + 100);
  assert.equal(a.state, 'thinking');
  assert.equal(a.thinkingSince, 1100);
  // 期间 showWaiting 不应回退
  a.showWaiting(1000 + WAIT_THRESHOLD_MS);
  assert.equal(a.state, 'thinking');
});

test('reasoning 追加累计，thinkingSince 只在首 delta 设', () => {
  const a = createActivity();
  a.arm(0);
  a.reasoning('甲', 10);
  a.reasoning('乙', 20);
  assert.equal(a.thinkingText, '甲乙');
  assert.equal(a.thinkingSince, 10);
});

test('visible 隐藏并清空 thinkingText', () => {
  const a = createActivity();
  a.arm(0);
  a.reasoning('想想', 10);
  a.visible();
  assert.equal(a.state, 'hidden');
  assert.equal(a.thinkingText, '');
  assert.equal(a.thinkingSince, null);
});

test('toolEnd 回到空窗重计', () => {
  const a = createActivity();
  a.arm(1000);
  a.reasoning('想', 1100);
  a.visible();
  a.toolEnd(2000);
  assert.equal(a.state, 'armed');
  assert.equal(a.emptySince, 2000);
});

test('end 全部归零', () => {
  const a = createActivity();
  a.arm(0);
  a.reasoning('想', 10);
  a.end();
  assert.equal(a.state, 'idle');
  assert.equal(a.thinkingText, '');
  assert.equal(a.emptySince, null);
});
```

- [ ] **Step 3: 跑测试**

Run: `node --test src/lib/activity.test.js`（工作目录 `crates/oc-http/ui`）
Expected: 6 passed。

- [ ] **Step 4: Commit**

```bash
git add crates/oc-http/ui/src/lib/activity.js crates/oc-http/ui/src/lib/activity.test.js
git commit -m "feat(web): 活动卡状态机（纯 JS，node --test）"
```

---

### Task 6: Web UI 接线（LiveActivity 组件 + ChatPane + api + state）

**Files:**
- Create: `crates/oc-http/ui/src/components/LiveActivity.vue`
- Modify: `crates/oc-http/ui/src/lib/api.js`（`sendChat` 加 `onReasoning`）
- Modify: `crates/oc-http/ui/src/lib/state.js`（加 `activities` / `activityFor`）
- Modify: `crates/oc-http/ui/src/components/ChatPane.vue`

**Interfaces:**
- Consumes: `createActivity` / `WAIT_THRESHOLD_MS`（Task 5）、`sendChat` 的 `onReasoning` 回调（本 task 内先加）。
- Produces: `activityFor(sessionId)`（state.js 导出），供 ChatPane 与将来的 StatusBar 使用。

- [ ] **Step 1: api.js 加 onReasoning 回调**

`crates/oc-http/ui/src/lib/api.js` 的 `sendChat`：在 JSDoc 参数表加一行 `@param {function} opts.onReasoning - (delta: string) → void`；在 switch 里加：

```js
            case 'reasoning':
              opts.onReasoning?.(data.delta ?? '')
              break
```

- [ ] **Step 2: state.js 加按会话的活动卡实例**

`crates/oc-http/ui/src/lib/state.js` 顶部 import 加 `createActivity`；文件靠 `activeChats` 附近加：

```js
// ── Per-session live activity (单例活动卡) ─────────────────────────────
// 按 sessionId 键入，与 activeChats 同构：ChatPane 无 :key，切换会话不重新
// 挂载，卡状态若放局部 ref 会在会话间串台。放进这里 back-keep 语义才成立。
const activities = reactive({})

export function activityFor(sessionId) {
  if (!activities[sessionId]) activities[sessionId] = createActivity()
  return activities[sessionId]
}
```

（`import { createActivity } from './activity.js'` 与已有 `import ... from './api.js'` 同区。）

- [ ] **Step 3: 写 LiveActivity.vue**

`crates/oc-http/ui/src/components/LiveActivity.vue`（完整）：

```vue
<script setup>
import { computed } from 'vue'

/**
 * 单例活动卡：不进 messageMap 的瞬时组件，渲染在消息流尾部。
 * 形态由 act.state 驱动（见 lib/activity.js 的状态机）。
 */
const props = defineProps({
  act: { type: Object, required: true },
})

const show = computed(() => props.act.state === 'waiting' || props.act.state === 'thinking')
const isThinking = computed(() => props.act.state === 'thinking')

// 展示开关：关闭时退化为只有标题 + 计时器（不渲染正文），
// 但卡不消失——「不知道模型在动」就等于退回静默。
const prefs = JSON.parse(localStorage.getItem('oc.liveActivity') || '{}')
const showText = (prefs.showThinking ?? true) === true

const seconds = computed(() => {
  const since = isThinking.value ? props.act.thinkingSince : props.act.waitingSince
  if (since == null) return 0
  return Math.max(0, Math.floor((Date.now() - since) / 1000))
})

const label = computed(() => (isThinking.value ? '💭 思考中' : '等待模型'))
</script>

<template>
  <div v-if="show" class="live" :class="{ waiting: !isThinking }">
    <div class="live-head"><span class="dot"></span>{{ label }} · {{ seconds }}s</div>
    <div v-if="isThinking && showText" class="live-body">{{ act.thinkingText }}<span class="caret"></span></div>
  </div>
</template>

<style scoped>
.live {
  border: 1px dashed color-mix(in srgb, var(--accent) 38%, var(--border));
  border-radius: var(--r-sm);
  background: color-mix(in srgb, var(--accent) 4%, transparent);
  padding: var(--sp-2) var(--sp-3);
  align-self: stretch;
}
.live.waiting { border-style: solid; border-color: var(--border); background: transparent; }
.live-head {
  display: flex; align-items: center; gap: 7px;
  font-size: 12px; font-weight: 600; color: var(--accent);
  font-variant-numeric: tabular-nums;
}
.live.waiting .live-head { color: var(--text-muted); font-weight: 500; }
.live-head .dot {
  width: 6px; height: 6px; border-radius: 50%;
  background: currentColor; animation: pulse 1.1s ease-in-out infinite;
}
.live-body {
  margin-top: 4px; font-size: 13px; line-height: 1.7; color: var(--text-muted);
  max-height: calc(1.7em * 6); overflow-y: auto; scrollbar-width: thin;
}
.live-body::-webkit-scrollbar { width: 4px; }
.live-body::-webkit-scrollbar-thumb { background: var(--border); border-radius: 2px; }
.caret {
  display: inline-block; width: 2px; height: 1em;
  vertical-align: text-bottom; background: var(--accent);
  animation: blink .9s step-end infinite;
}
@keyframes pulse { 0%,100% { opacity:.35; transform: scale(.85) } 50% { opacity:1; transform: scale(1) } }
@keyframes blink { 0%,100% { opacity:1 } 50% { opacity:0 } }
</style>
```

- [ ] **Step 4: ChatPane 接线**

`crates/oc-http/ui/src/components/ChatPane.vue`，改动五处：

**(a)** import 区加：

```js
import LiveActivity from './LiveActivity.vue'
import { activityFor, WAIT_THRESHOLD_MS } from '../lib/state.js'
```

（`WAIT_THRESHOLD_MS` 需从 `state.js` re-export：在 state.js 的 import 行 `import { createActivity } from './activity.js'` 改成 `import { createActivity, WAIT_THRESHOLD_MS } from './activity.js'`，并 `export { WAIT_THRESHOLD_MS }`。或 ChatPane 直接从 `../lib/activity.js` import 阈值——选后者更直接，避免污染 state.js 的导出面。）

**(b)** setup 里加等待定时器管理：

```js
const waitTimers = new Map()   // sessionId -> timeout id

function scheduleWait(sessionId) {
  clearTimeout(waitTimers.get(sessionId))
  const t = setTimeout(() => {
    const act = activityFor(sessionId)
    act.showWaiting(Date.now())
  }, WAIT_THRESHOLD_MS)
  waitTimers.set(sessionId, t)
}
```

**(c)** `submit()` 里，`appendMessage` 用户气泡之后、`sendChat` 之前加：

```js
  const act = activityFor(target)
  act.arm(Date.now())
  scheduleWait(target)
```

**(d)** `sendChat` 回调加 `onReasoning` 与扩展 `onDelta`/`onTool`/`onEnd`/`onError`：

```js
    onReasoning(delta) {
      clearTimeout(waitTimers.get(target))
      activityFor(target).reasoning(delta, Date.now())
    },
    onDelta(delta) {
      activityFor(target).visible()
      updateLastAssistant(target, delta)
    },
    onTool(ev) {
      if (ev.phase?.phase === 'start') {
        activityFor(target).visible()
        clearTimeout(waitTimers.get(target))
        // ... 原 appendMessage(tool) 逻辑不变 ...
      } else if (ev.phase?.phase === 'update') {
        // ... 原样 ...
      } else if (ev.phase?.phase === 'end') {
        // ... 原样（更新 toolStatus）...
        activityFor(target).toolEnd(Date.now())
        scheduleWait(target)
      }
    },
    onEnd() {
      activityFor(target).end()
      clearTimeout(waitTimers.get(target))
      finalizeLastAssistant(target)
      clearActiveChatCtrl(target)
    },
    onError(msg) {
      activityFor(target).end()
      clearTimeout(waitTimers.get(target))
      finalizeLastAssistant(target)
      clearActiveChatCtrl(target)
      errorText.value = msg
    },
```

（`onTool` 的 start/update/end 原有逻辑保留，只在 start 处加 `visible()`、end 处加 `toolEnd`+`scheduleWait`。`visible()` 在 start 里调用、`onDelta` 里也调用，二者互不冲突——谁先到谁把卡藏了，后到的 `visible()` 是幂等的 no-op。）

**(e)** 模板：在 `<template v-else>` 的消息循环之后、`</div>`（`.messages`）之前插入：

```vue
      <LiveActivity :act="activityFor(props.sessionId)" />
```

（`activityFor` 已在 import 中，模板可直接调用。注意它返回 reactive proxy，`act.state` 变化会触发组件重渲染。）

**(f)** `stop()` 函数加清理：

```js
function stop() {
  clearTimeout(waitTimers.get(props.sessionId))
  activityFor(props.sessionId).end()
  activeChats.get(props.sessionId)?.abort()
  clearActiveChatCtrl(props.sessionId)
  finalizeLastAssistant(props.sessionId)
}
```

- [ ] **Step 5: 自动滚动把卡算进去**

`ChatPane.vue` 现有的 `watchEffect`（`// Autoscroll when...` 注释处）只依赖 `messages.value.length` 与尾条 `content.length`。在函数体最前面读一次活动卡状态，把它变成依赖：

```js
watchEffect(() => {
  const len = messages.value.length
  const tail = messages.value.at(-1)?.content?.length
  const act = activityFor(props.sessionId)
  const _ = act.state + act.thinkingText.length   // 卡的出现/形态切换也触发滚动
  if (scroller.value) {
    requestAnimationFrame(() => {
      scroller.value.scrollTop = scroller.value.scrollHeight
    })
  }
})
```

（`act.thinkingText.length` 作为依赖会让每个 reasoning delta 都滚一次；卡已限高 6 行，滚动的开销可接受，且 delta 时保持贴底正是期望行为。）

- [ ] **Step 6: 构建验证**

Run: `npm run build`（工作目录 `crates/oc-http/ui`）
Expected: 构建成功，无 Vue 模板编译错误。

- [ ] **Step 7: 跑前端测试确认未破坏**

Run: `node --test src/lib/activity.test.js`（工作目录 `crates/oc-http/ui`）
Expected: 6 passed。

- [ ] **Step 8: Commit**

```bash
git add crates/oc-http/ui/src/lib/api.js crates/oc-http/ui/src/lib/state.js \
        crates/oc-http/ui/src/components/LiveActivity.vue \
        crates/oc-http/ui/src/components/ChatPane.vue
git commit -m "feat(web): 单例活动卡接线——waiting/thinking 实时可见"
```

---

### Task 7: StatusBar 死指示灯修复

**Files:**
- Modify: `crates/oc-http/ui/src/components/StatusBar.vue`

**Interfaces:**
- Consumes: `activeChats`（state.js 已有，`shallowReactive(new Map())`）。

- [ ] **Step 1: 派生 isRunning**

`StatusBar.vue` 的 script 里加 import 与 computed：

```js
import { status, activeChats } from '../lib/state.js'
```

并加：

```js
// 本 Web 客户端自己发起的 run 是否还在进行。不用 status.active_run：
// 那个字段只在 ambient SSE 开场 snapshot 赋值一次，之后永不更新（死指示灯）。
const isRunning = computed(() => activeChats.size > 0)
```

- [ ] **Step 2: 模板替换**

把模板里的：

```vue
    <span v-if="status.active_run" class="chip running">
```

改为：

```vue
    <span v-if="isRunning" class="chip running">
```

（其余不动。`status.active_run` 字段仍在 state.js 里，但不再被 UI 消费。）

- [ ] **Step 3: 构建验证**

Run: `npm run build`（工作目录 `crates/oc-http/ui`）
Expected: 构建成功。

- [ ] **Step 4: Commit**

```bash
git add crates/oc-http/ui/src/components/StatusBar.vue
git commit -m "fix(web): 状态栏「生成中」从 activeChats 派生，修死指示灯"
```

---

### Task 8: TUI 状态栏滚动

**Files:**
- Modify: `crates/oc-tui/Cargo.toml`
- Modify: `crates/oc-tui/src/app.rs`

**Interfaces:**
- Consumes: `Event::Reasoning`（Task 1 已加空分支，本 task 替换为滚动逻辑）。
- Produces: `fn tail_display(s: &str, max_cols: usize) -> String`（模块内 helper，单测覆盖）。

- [ ] **Step 1: 加 unicode-width 依赖**

`crates/oc-tui/Cargo.toml` 的 `[dependencies]` 加：

```toml
unicode-width = "0.1"
```

（Cargo.lock 已有 0.1.14，不会引入新版本。中文双宽必须按显示列宽截断，字符数会错位。）

- [ ] **Step 2: App 加字段**

`crates/oc-tui/src/app.rs` 的 `pub struct App` 加两个字段：

```rust
    /// thinking 模型的推理内容尾部（状态栏滚动显示）。有界，只留末尾。
    reasoning_tail: String,
    /// 是否渲染 reasoning（键位切换；仅控制显示，事件照收）。
    show_reasoning: bool,
```

并在 `App::connect` 的 `Self { ... }` 初始化里加：

```rust
            reasoning_tail: String::new(),
            show_reasoning: true,
```

- [ ] **Step 3: 状态栏渲染**

`draw()` 里 `chunks[2]` 的 `status_line` 逻辑，在现有 `match &self.usage_hint` 之前插入 thinking 分支。当前代码：

```rust
        let status_line = match &self.usage_hint {
            Some(u) => format!("{}  |  ctx {}", self.status, u),
            None => self.status.clone(),
        };
        let status = Paragraph::new(status_line).style(Style::default().fg(Color::DarkGray));
        f.render_widget(status, chunks[2]);
```

改为：

```rust
        // thinking 活跃时状态栏让位给推理滚动；否则沿用原有状态 + 用量。
        let status_line = if self.show_reasoning && !self.reasoning_tail.is_empty() {
            let max_cols = chunks[2].width.saturating_sub(3) as usize; // 留 💭 前缀
            format!("💭 {}", tail_display(&self.reasoning_tail, max_cols))
        } else {
            match &self.usage_hint {
                Some(u) => format!("{}  |  ctx {}", self.status, u),
                None => self.status.clone(),
            }
        };
        let status = Paragraph::new(status_line).style(Style::default().fg(Color::DarkGray));
        f.render_widget(status, chunks[2]);
```

- [ ] **Step 4: helper 函数**

`app.rs` 文件末尾（`format_usage` 函数附近）加：

```rust
/// 取字符串的末尾 `max_cols` 个显示列宽（中文双宽按列算，不按字符数）。
/// delta 持续追加、显示始终取尾部，天然形成左滚，无需动画定时器。
fn tail_display(s: &str, max_cols: usize) -> String {
    use unicode_width::UnicodeWidthStr;
    if s.width() <= max_cols {
        return s.to_string();
    }
    let skip = s.width() - max_cols; // 从左边跳过的列宽
    let mut cols = 0usize;
    let mut start = s.len();
    for (i, c) in s.char_indices() {
        let w = c.width().unwrap_or(0);
        if cols + w > skip {
            start = i;
            break;
        }
        cols += w;
        start = i + c.len_utf8();
    }
    s[start..].to_string()
}
```

- [ ] **Step 5: 事件分支替换**

把 Task 1 加的 `Event::Reasoning { .. } => {}` 空分支替换为：

```rust
            Event::Reasoning { delta, .. } => {
                if !self.show_reasoning {
                    return;
                }
                // 换行压成空格，避免推理内容里带进换行把状态栏撑成多行。
                self.reasoning_tail.push_str(&delta.replace(['\r', '\n'], " "));
                // 有界：只留末尾，防长推理无限增长。
                const TAIL_CAP: usize = 512;
                if self.reasoning_tail.chars().count() > TAIL_CAP {
                    let skip = self.reasoning_tail.chars().count() - TAIL_CAP;
                    self.reasoning_tail = self.reasoning_tail.chars().skip(skip).collect();
                }
            }
```

- [ ] **Step 6: 清空条件**

在 `handle_event` 的 `Event::Assistant` 分支开头、以及 `LifecyclePhase::End` / `LifecyclePhase::Error` 分支里清空 `reasoning_tail`：

`Event::Assistant` 分支的 `let same_run = ...` 之前加：

```rust
                self.reasoning_tail.clear();
```

`LifecyclePhase::End` 与 `LifecyclePhase::Error { .. }` 分支里，`self.pending_queue_hint = None;` 之后各加一行：

```rust
                    self.reasoning_tail.clear();
```

（`ToolPhase::Start` 分支里也清一次——一个 step 首 token 是工具时，思考段随之结束，与 Web 的 `visible()` 同构。在 `Event::Tool { phase, .. } => match phase` 的 `ToolPhase::Start { .. }` 臂开头加 `self.reasoning_tail.clear();`。）

- [ ] **Step 7: 键位切换**

`handle_key`（按键处理函数，约 `app.rs:170-260`）里，找一个不冲突的按键。已占用：`Ctrl-C`（退出）、`y/n`（审批态）、`Enter`/`Esc`/`Backspace`/`PageUp`/`PageDown`/`Ctrl-Home`/`Ctrl-End`、普通文本字符。加 `Ctrl-T` 切换（`t` 单独会被输入框吃掉，必须带 modifier）。在非审批/非输入态的分支里加：

```rust
            KeyCode::Char('t') if mods.contains(KeyModifiers::CONTROL) => {
                self.show_reasoning = !self.show_reasoning;
                if !self.show_reasoning {
                    self.reasoning_tail.clear();
                }
            }
```

（放在 `KeyCode::Char(c) if is_text_char(mods)` 分支**之前**，否则普通 `t` 仍进文本。注意 Ctrl-T 不会被 `is_text_char` 匹配，因为 `is_text_char` 排除了含 modifier 的键。把这条臂放在 `KeyCode::PageDown` 附近的输入态 match 里——具体位置以「在文本输入臂之前、且在非审批分支内」为准。）

同时在 `App::connect` 里 `push_sys` 的欢迎语补一句可发现性提示：

```rust
                    app.push_sys("已连接到 oc daemon。输入消息回车发送，/help 看指令，Ctrl-T 开关思考显示，Ctrl-C 退出。");
```

- [ ] **Step 8: 单测**

`app.rs` 末尾加：

```rust
#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn tail_display_keeps_tail_columns() {
        // "你好世界" = 8 列宽（每字 2）。取尾 4 列应得 "好世"？不——"你好世界" 四字各 2 列，
        // 尾 4 列 = 后两个字 "世界"。
        assert_eq!(tail_display("你好世界", 4), "世界");
        // 全 ASCII，尾 3 = "xyz"
        assert_eq!(tail_display("abcdefxyz", 3), "xyz");
    }

    #[test]
    fn tail_display_short_enough_unchanged() {
        assert_eq!(tail_display("短", 10), "短");
        assert_eq!(tail_display("abc", 3), "abc");
    }

    #[test]
    fn tail_display_handles_mixed_width() {
        // "a好b" = 1 + 2 + 1 = 4 列。尾 3 列 = "好b"。
        assert_eq!(tail_display("a好b", 3), "好b");
    }
}
```

（`tail_display("你好世界", 4)` 期望 `"世界"`：`"你好世界"` 宽 8，skip=4，从左累计到 `你好` 满 4 列后 start 落在 `世`，取 `"世界"`。确认与 helper 实现一致。）

- [ ] **Step 9: 跑测试 + clippy**

Run: `cargo test -p oc-tui`
Expected: 全绿（新增 3 个 tail_display 测试 + 既有）。

Run: `cargo clippy -p oc-tui --all-targets -- -D warnings`
Expected: 无警告（尤其注意 `reasoning_tail` 的 `chars().count()` 重复计算——若 clippy 提示，把 TAIL_CAP 那段的 count 先存进局部变量）。

- [ ] **Step 10: Commit**

```bash
git add crates/oc-tui/Cargo.toml crates/oc-tui/src/app.rs
git commit -m "feat(tui): 状态栏滚动显示 reasoning，Ctrl-T 开关"
```

---

### Task 9: 协议文档（第三方接入）

**Files:**
- Modify: `docs/reference/protocol.md`

**Interfaces:**
- 无代码依赖。只把 spec 的 F 节落进对外契约文档。

- [ ] **Step 1: 写 F 节**

在 `docs/reference/protocol.md` 末尾追加（标题「第三方 / app 客户端接入 thinking」）：

```markdown
## 第三方 / app 客户端接入 thinking

本仓库之外的客户端走 **native HTTP API**（`/api/v1/*`），不是 socket——socket 只在本机。
`oc http --bind <非 loopback> --token <t>` 对外提供；绑非 loopback 时 token 强制
（`auth.rs::check_bind_requires_token`，建 socket 前校验）。

### 线上格式

`Event` 是 `#[serde(tag = "event", rename_all = "snake_case")]`，外层 `Frame` 是
`tag = "kind"`：

    # NDJSON（socket，本机客户端）
    {"kind":"event","event":"reasoning","session":"main","run_id":"...","delta":"我需要先确认"}

    # HTTP SSE（POST /api/v1/chat/send 的响应体）
    event: reasoning
    data: {"event":"reasoning","session":"main","run_id":"...","delta":"我需要先确认"}

### 接入步骤

1. `POST /api/v1/chat/send`，带 `Authorization: Bearer <token>`、
   `Accept: text/event-stream`，body `{"session":"...","text":"..."}`。
2. 按 SSE 的 `event:` 名分发：`accepted` / `reasoning` / `assistant` / `tool` /
   `approval` / `user_input` / `lifecycle`。
3. **忽略不认识的 `event:` 名**——前向兼容的义务在客户端侧，服务端不做能力协商，
   新增事件不会事先通知。

### 四条语义契约

- **reasoning 只在 `chat/send` 的 POST 响应里，不在 `GET /api/v1/events` 上。**
  它是 run 的内联事件，经 `RunSink::Conn` 直达发起 turn 的连接，从不进全局广播；
  ambient 流只有 `Usage` / `Proactive` / `Task`。
- **一个 run 内有多段 reasoning，每 step 一段，无显式段起止标记。**
  段边界由「本 step 首个其他事件到达」隐式给出（`assistant` delta 或 `tool` start）。
- **非 thinking 模型永远不发 reasoning。** UI 必须在没有它的情况下也说得通。
- **不落库。** `GET /api/v1/sessions/:id/history` 里没有 reasoning，永远不会有。

`/v1/responses`（OpenAI 兼容层）不提供 thinking——Responses 协议没有对应事件。
需要 thinking 的客户端必须走 native API。
```

- [ ] **Step 2: 核对与 schema 一致性**

Run: `cargo test -p oc-proto`
Expected: schema 导出测试仍绿（`Reasoning` 已进 schema，`oc doctor --dump-schema` 会带上它）。

- [ ] **Step 3: Commit**

```bash
git add docs/reference/protocol.md
git commit -m "docs(protocol): thinking 接入的线上格式与语义契约"
```

---

## 完成后的整体验证

- [ ] `cargo test --workspace --locked`（全 Rust 测试，含新增 5 个测试文件/函数）
- [ ] `cargo clippy --workspace --all-targets -- -D warnings`
- [ ] `node --test src/lib/activity.test.js`（`crates/oc-http/ui` 目录）
- [ ] `npm run build`（`crates/oc-http/ui` 目录，产出 `dist/` 供 `rust-embed` 嵌入）

---

## Self-Review 记录

- **Spec 覆盖**：A（Task 1）、E 解码兜底（Task 2）、B 的 SSE 契约（Task 3）、
  `/v1/responses` 静默（Task 4）、B 状态机（Task 5）、B 组件+接线+会话隔离+自动滚动
  （Task 6）、D 死指示灯（Task 7）、C TUI 滚动（Task 8）、F 文档（Task 9）。全覆盖。
- **会话隔离**：Task 6 Step 2 的 `activities` 按 sessionId 键入；测试点在 Task 6
  未单列自动化（Vue 组件层无测试设施），改由 `npm run build` + 状态机单测兜底，
  并在代码注释里点明「不能放局部 ref」的理由。
- **类型一致性**：`createActivity` 方法名 `arm/showWaiting/reasoning/visible/toolEnd/end`
  在 Task 5（定义）与 Task 6（调用）完全一致；`WAIT_THRESHOLD_MS` 一致；
  `activityFor` 在 state.js（Task 6 Step 2）定义、ChatPane 导入。
